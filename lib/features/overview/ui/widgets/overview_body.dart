import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class OverViewBody extends StatelessWidget {
  const OverViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('OverViewBody'),
    );
  }
}
